import 'package:flutter/material.dart';

class ListsUsers extends StatelessWidget {
  const ListsUsers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Usuarios'),
      ),
      body: Container(),
    );
  }
}
